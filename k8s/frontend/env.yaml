apiVersion: v1
kind: ConfigMap
metadata:
  name: config-map
  namespace: application
data:
  REACT_APP_API_URL: "http://backend-svc.application.svc.cluster.local"
  NODE_ENV: "production"

---
apiVersion: v1
kind: Secret
metadata:
  name: secret-map
  namespace: application
type: Opaque
stringData:
  # Example only (if needed)
  # REACT_APP_API_KEY: "your-secret-key"
  DUMMY: "not-used"
